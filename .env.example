# PROVENIQ — REQUIRED SERVER CONFIG (TEMPLATE)
# This file is non-secret. Copy to .env.local (or .env) and fill values.

# --- Environment ---
PROVENIQ_ENV=dev

# --- Database (Supabase Postgres) ---
# Use pooled connection for runtime if needed; use direct for migrations.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME
DIRECT_DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME

# --- Firebase Admin (Identity Provider) ---
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# IMPORTANT: keep quotes; newline escapes may be required depending on runtime.
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# --- Service Config ---
PORT=8001

# --- Properties-specific ---
PROPERTIES_UPLOAD_BUCKET=proveniq-properties-dev
GCS_PROJECT_ID=
